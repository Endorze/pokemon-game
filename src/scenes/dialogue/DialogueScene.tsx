import { SceneComponent, SceneProps } from "src/shared/types/types"

const DialogueScene: SceneComponent = (props: SceneProps) => {
    return <div>Dialogue Scene</div>
}

export default DialogueScene